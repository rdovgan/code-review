# Central project registry for the Code Review Bot
#
# Each key under "projects" is a Bitbucket repo full name: workspace/repository-slug
# Settings here are the BASE config for that project.
# If the repo also has a .codereview.yml, its values OVERRIDE these.
# If a field is omitted, the system default applies.
#
# Credentials (username, app_password, webhook_secret) are stored per-workspace
# in config/credentials.yml — not here.
#
# To add a new project: copy any block below, change the repo key + settings,
# then add a webhook in Bitbucket. No restart required.


# Java team Mattermost notifications — shared across all Java repos below.
# The actual webhook URL is a secret and lives in .env (MATTERMOST_WEBHOOK_URL),
# not here — this only lists who triggers a notification.
# notify_authors matches PRContext.author, which for Bitbucket is the PR actor's
# display name from the webhook payload (Bitbucket does not expose email here) —
# confirm these match the real Bitbucket display names.
x-java-team-notify: &java_team_notify
  notify_authors:
    - "Roman Dovgan"
    - "Olena Syrotenko"
    - "Oleksii Kravchenko"
    - "Oleksandr Farion"
    - "Ihor Romaniuk"

projects:

# ── Java applications ──────────────────────────────────────────────────

  mybookingpal/mbp:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: false
    block_merge_on:
      - CRITICAL
    max_diff_lines: 5000
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
      - "**/*.log"
      - "**/*.csv"
      - "**/*.xlsx"
      - ".idea/**"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/channel-batch-jobs:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
      - "**/*.log"
      - "**/*.csv"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/channel-integration:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 1500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
      - "**/*.log"
      - "**/*.csv"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/core-module:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 4000
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/dataaccesslayer:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/revpal:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/redis:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/payment:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 1500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
      - "**/*.log"
      - "**/*.png"
      - "**/*.xml"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/cassandra:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/sql-injection
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/mbp-utils:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 1000
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/cloud-services:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/secrets
    ai_focus:
      - security
      - bugs

  mybookingpal/parent-dal:
    <<: *java_team_notify
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: java
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "**/*.properties"
      - "**/*.md"
      - "**/*.sh"
    semgrep_rules:
      - owasp
      - security-audit
      - p/java
      - p/findsecbugs
      - p/secrets
    ai_focus:
      - security
      - bugs

# ── Python applications ───────────────────────────────────

  mybookingpal/image-tool:
    target_branches: ["master", "main", "demo-dev", "demo-stable"]
    language: python
    ai_review: true
    static_analysis: true
    block_merge_on:
      - CRITICAL
    max_diff_lines: 2500
    ignore_paths:
      - "migrations/*"
      - "tests/*"
      - "**/*_test.py"
    semgrep_rules:
      - owasp
      - security-audit
      - p/python
      - p/django
    ai_focus:
      - security
      - bugs


  # ── Example: React / TypeScript frontend ───────────────────────────────────
#  myworkspace/my-react-frontend:
#    language: js
#    ai_review: true
#    static_analysis: true
#    block_merge_on:
#      - CRITICAL
#    max_diff_lines: 400
#    ignore_paths:
#      - "dist/*"
#      - "build/*"
#      - "**/*.min.js"
#      - "node_modules/*"
#    semgrep_rules:
#      - owasp
#      - p/javascript
#    ai_focus:
#      - security
#      - bugs

  # ── Example: C# / .NET service ─────────────────────────────────────────────
#  myworkspace/my-dotnet-service:
#    language: dotnet
#    ai_review: true
#    static_analysis: true
#    block_merge_on:
#      - CRITICAL
#      - BUG
#    max_diff_lines: 600
#    ignore_paths:
#      - "**/*.Designer.cs"
#      - "**/Migrations/*"
#      - "**/*.g.cs"
#    semgrep_rules:
#      - owasp
#      - p/csharp
#    ai_focus:
#      - security
#      - bugs

  # ── Example: PHP application ───────────────────────────────────────────────
#  myworkspace/my-php-app:
#    language: php
#    ai_review: true
#    static_analysis: true
#    block_merge_on:
#      - CRITICAL
#    max_diff_lines: 500
#    ignore_paths:
#      - "vendor/*"
#      - "storage/*"
#      - "bootstrap/cache/*"
#    semgrep_rules:
#      - owasp
#      - p/php
#    ai_focus:
#      - security
#      - bugs
